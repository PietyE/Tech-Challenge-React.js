import type { SxProps } from '@mui/material';

export const styles: Record<'imageContainer', SxProps> = {
  imageContainer: {
    height: '350px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
};
